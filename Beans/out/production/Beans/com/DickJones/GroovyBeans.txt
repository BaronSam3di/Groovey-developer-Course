A JavaBEan is just a standard

- All properties private (use getters/setters)
- A public no-argument constructor
-Implements Serializable.
    Java provide a mechanism, called object serialization where an object can be represented as a
    sequence of bytes that includes the object's data as we; as information about an object's
    type and the types of data stored in the object.


TODO
- Create a Java Bean
- Equivalent groovy Bean
- Look at a groovy BEan under the hood
- How to use groovy Beans
- How to write your own getters / setters
- Direct field access